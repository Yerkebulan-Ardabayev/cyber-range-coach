Проект: /Users/yerkebulan/Desktop/Projects/cyber-range-coach

Реализовать ТОЛЬКО Этап 1 расширения v2.1 «Повторение команд из CyberLabs» строго по разделу 9 канонического spec.md (он уже в дереве, незакоммичен, изменения владельца читать как контракт). Сначала прочитай spec.md раздел 9 целиком (9.1 Проверенный контекст, 9.2 Части, 9.3 Контракты, 9.4 Данные, 9.5 Границы, 9.6 Этапы, 9.7 Готово-когда). Этапы 2 и 3 из 9.6 НЕ начинать.

ИСТОЧНИК КОМАНД (только чтение, не редактировать оригиналы):
- 11 текстовых Linux-мнемоник: /Users/yerkebulan/Desktop/СуberLabs/Linux/ , файлы вида «NN · Название · мнемоника.docx», номера 01-07 и 09-12 (Linux 08 отсутствует, не выдумывать).
- Команды лежат в параграфах и таблицах DOCX, встроенных изображений нет. Извлекать текстом через python-docx (paragraphs + tables). OCR и изображения в Этапе 1 не нужны.

ЖЁСТКИЕ ОГРАНИЧЕНИЯ, нарушать нельзя:
- Только Этап 1. Без миссий-расследований, без Windows-практики, без скриншотов, OCR и извлечения изображений. Это этапы 2 и 3.
- Существующие 19 уроков, backend/src/cyber_range_coach/services/grader.py и transcript/probe-логику НЕ трогать и НЕ ослаблять. Только добавлять новый код рядом.
- Не ставить новые зависимости. Всё нужное уже есть: alembic, sqlalchemy, pyyaml, python-docx, fastapi, pydantic. Если кажется, что нужна новая зависимость, остановись и опиши причину в отчёте, не добавляй.
- Git не коммитить и не пушить, это делает владелец.
- Миграцию проверять на ОДНОРАЗОВОЙ КОПИИ базы. Рабочую базу и Windows не трогать. Миграция строго аддитивная: только CREATE новых таблиц, существующие таблицы и данные не менять; downgrade удаляет только новые таблицы.
- Контент под git только обезличенный: реальные пароли, найденные флаги, реальные хэши и пути пользователя в git не попадают, значения в заданиях синтетические. Прогнать scripts/secret_scan.py по добавленному контенту.
- Ноль длинных тире в любом тексте, который увидит пользователь и в контенте.

ЭТАП 1, пункты:

1. Каталог приёмов (контент под git).
- Извлечь Linux-команды-приёмы из 11 мнемоник. Приём это команда в связке с оболочкой и назначением. Linux find и рекурсивный grep это разные приёмы; grep для текста и рекурсивный поиск имеют отдельные учебные цели, но общее семейство.
- Модель контента CommandTechnique (поля): устойчивый id, семейство команды, оболочка shell, задача/назначение, значимые флаги, типичная ошибка, образ из мнемоники, ссылки на источник (имя файла + адрес абзаца или строки таблицы), версия.
- Хранить как версионируемый YAML в стиле curriculum/fundamentals.yaml (новый файл в curriculum/ или подпапке). Загрузчик по аналогии с services/curriculum.py (Curriculum), техники в память, состояние в БД.
- SourceCoverage: таблица покрытия, каждому исходному пункту статус: включён; объединён с указанным приёмом; только справочный; нужен разбор изображения; нужен отдельный стенд; секрет или ответ лабы, исключён. Нельзя молча потерять пункт. Предположение парсера не считать подтверждённой командой: спорные пометить статусом на ручную проверку.
- Валидатор контента (по образцу tools/validate_content.py): схема техник, ссылочная целостность, полнота покрытия, ненулевой код при ошибке.

2. Модель данных в SQLite (аддитивно).
- Новые таблицы: CommandPracticeState (на приём: статусы вспоминал без помощи / с помощью / разбирал вывод / применял в среде / применял в другом варианте; версия задания и данных; следующий срок; часовой пояс явно), CommandAttempt (дата, оболочка, ответ, раскрытая помощь, вид ошибки, результат, вид доказательства).
- Аддитивная Alembic-миграция (следующая ревизия после frozen 20260820_0001). Проверить на копии базы: upgrade создаёт только новые таблицы, downgrade их удаляет, PRAGMA integrity_check и подтверждение ревизии проходят.

3. Recall grader (воспроизведение команды), отдельный модуль, не grader.py.
- Вход: ответ ученика и контракт конкретного задания. Ответ НИКОГДА не исполняется как shell-команда.
- Проверка учитывает кавычки (пробелы внутри кавычек значимы, не схлопывать) и регистр Linux-команд (регистрозависимо). Общую нормализацию пробелов и IGNORECASE не применять.
- Допустимые варианты (например перестановка независимых флагов) заданы в контракте задания, а не общей нормализацией.
- Выход: одна различимая причина из набора: верно; верно с помощью; неверный инструмент; неверный флаг; другая оболочка; недостаточно данных.
- Отдельный вопрос «что означает эта строка вывода» проверяет понимание наблюдения, отделён от синтаксиса.

4. Практика и интервальное повторение.
- Задание «вспомни команду» со ступенями подсказок: смысл приёма и образ; название команды; скелет с пропусками; полный пример с объяснением. Ученик может выбрать «Не помню». Факт раскрытия каждой ступени сохраняется на сервере.
- Интервалы после самостоятельных верных ответов: 1, 3, 7, 14, 30 дней (проектные значения). Подсказанный ответ не продвигает самостоятельное воспроизведение. Ошибка возвращает приём позже в текущей сессии, если есть время, и на следующий день. История реального применения не обнуляется из-за одной ошибки памяти.
- Повторная отправка того же завершения не создаёт дубль. Черновик ответа сохраняется при уходе со страницы.

5. Планировщик.
- Сделать приоритет просроченного БЕЗУСЛОВНЫМ. Сейчас в services/curriculum.py Curriculum.plan просроченное лишь тай-брейк в кортеже score (первым идёт сумма минут). Просроченное и забытое должны идти раньше заполнения времени новым материалом, при равенстве раньше назначенное повторение.
- Для подбора повторений команд использовать greedy по (просрочено desc, due_at asc), затем добор нового материала в остаток. Без combinations. Ограничить объём одной сессии, показывать оставшийся долг.

6. Frontend.
- Страница или панель повторения команд со ступенями подсказок, точка входа из Today (frontend/src/pages/TodayPage.tsx). Панель каталога приёма доступна рядом с заданием, без обязательного перехода в отдельный справочник. Адаптив как в проекте, роли и CSRF как в существующих мутациях. Ввод ответа не исполняется, проверка на сервере.

ПРИЁМКА, обязательна перед завершением (проверяемый результат):
- scripts/quality.sh проходит целиком: ruff, mypy, pytest, secret_scan, bash -n, eslint, typecheck, vitest, build, playwright.
- Новые адресные тесты: recall grader (регистр значим, кавычки значимы, разрешённая перестановка флагов, «не помню», подсказка не продвигает самостоятельное воспроизведение); интервалы 1/3/7/14/30; безусловный приоритет просроченного в планировщике; аддитивная миграция на копии базы (upgrade/downgrade/integrity).
- Секрет-скан добавленного контента чистый.

Отчитайся фактами: список добавленных и изменённых файлов, список новых тестов, полный итог прогона scripts/quality.sh. Не заявлять успех без фактического прогона. Если упёрся в решение, требующее владельца (новая зависимость, запись в рабочую базу, git), остановись и опиши, не обходи.
