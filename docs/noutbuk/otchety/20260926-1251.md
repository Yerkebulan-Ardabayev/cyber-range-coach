# Отчёт с ноутбука 26.09.2026 12:51

Установщик 2d8c2c2 (run 36226716390, SHA-256 `419f28f6…dfab30f61` совпал), поверх запущенной 1a20c31.

## Итог

**Relay активной лабы восстанавливается после перезапуска академии.** После «Снять задачу» и нового запуска `nc -vz 172.30.160.1 47000`
в той же лабе даёт `succeeded!` без «Начать чистую попытку». Lab run 5 `completed / passed / verified`.

## 1. Установка

`install.log`: `12:47:25 Log opened` → `12:47:41 RestartManager found no applications using one of our files.` → `12:47:51 Installation process succeeded.`
«закрываем принудительно» нет. `academy.log`: `12:47:27,866 Получен запрос остановки` → `12:47:33,081 Finished server process [1508]` (5,2 с).

## 2. Лаба до перезапуска

Академия PID 10532. Урок «TCP-достижимость учебной цели», цель `cyberlab-juice-shop`, lab run 5 (`POST /api/v2/lab-runs 201` в 12:49:01, `Command: relay 47000`).
```
bash-5.2$ nc -vz 172.30.160.1 47000
Connection to 172.30.160.1 47000 port [tcp/*] succeeded!
```
```
LocalAddress LocalPort  State OwningProcess
172.30.160.1     47000 Listen         10532
```

## 3. Перезапуск без завершения лабы

`Stop-Process -Id 10532 -Force` (= «Снять задачу»). Через 5 с порт 47000 не слушается (`after kill listen: False`).
Запуск из меню Пуск, новый PID 15912. Сразу после старта открытая страница `/lab/5` сама переподключила терминал:
```
2026-09-26 12:49:36,145 INFO uvicorn.error Started server process [15912]
2026-09-26 12:49:36,527 INFO uvicorn.error 127.0.0.1:55676 - "WebSocket /api/v2/terminal/5" [accepted]
```
Через 25 с после запуска relay уже слушал, до ручного открытия лабы:
```
LocalAddress LocalPort RemoteAddress  State OwningProcess
172.30.160.1     47000 0.0.0.0       Listen         15912
```

## 4. Та же лаба после перезапуска

Открыл `/lab/5` заново (WebSocket 12:50:11). Без «Начать чистую попытку»:
```
bash-5.2$ nc -vz 172.30.160.1 47000
Connection to 172.30.160.1 47000 port [tcp/*] succeeded!
```

## 5. Проверка до конца

`12:50:50 Command: relay 47000` (probe поднял или проверил relay), `POST /api/v2/lab-runs/5/grade 200`.
Проверки: input_integrity, command, attempt_boundary, required ×2, forbidden все `true`. Объяснение, наставник, debrief, `12:51:32 POST …/finalize 200`.
Итог lab run 5: `status: completed`, `grader_status: passed`, `input_integrity: verified`.

## Замечания

- `terminal_inputs` лабы 5 содержит одну строку `nc -vz 172.30.160.1 47000`: ввод до перезапуска в журнал попытки не попал (новая SSH-сессия после перезапуска). Зачёт при этом корректный, `attempt_boundary` true. Если так задумано, всё в порядке.
- Сообщение «Связь с учебной целью не восстановлена» не появлялось: порт был свободен.
- `ERROR uvicorn.error Cancel 0 running task(s), timeout graceful shutdown exceeded` по-прежнему пишется уровнем ERROR при штатной остановке.

## Что поменялось на ноутбуке

- Академия обновлена до 2d8c2c2, данные на месте.
- Засчитан lab run 5 (tcp-reachability): итог lab 1, 2, 4, 5 passed, 3 abandoned, активных нет.
- Docker, брандмауэр, `.wslconfig`, контролируемый доступ к папкам не трогал. Контейнер `cyberlab-webgoat` в `docker ps` показан `unhealthy` (я его не трогал, для этого урока не нужен).
