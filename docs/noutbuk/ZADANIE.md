<context>
Ты на Windows-ноутбуке-полигоне Cyber Range Coach. Правила машины в `%USERPROFILE%\.claude\CLAUDE.md`, правила проекта в `CLAUDE.md` репозитория. Прошлый отчёт `docs/noutbuk/otchety/20260925-1516.md`: мастер WSL и урок TCP пройдены, найдены 3 ошибки.

Что исправлено на Mac 25.09 по этому отчёту (коммиты `11b0840`, `06947da`, `9748358`):
- Академия сама держит WSL Ubuntu включённой: один скрытый `wsl.exe -d Ubuntu --exec sleep infinity` на всё время работы академии, привязанный к ней (умирает вместе с ней, даже при taskkill). Перед каждым SSH ждёт, пока в Ubuntu поднимется sshd.
- «Проверить SSH» выбирает ключ ed25519 и пишет команду сверки по типу ключа.
- Ошибки «Проверить SSH» и «Проверить range-runner» показываются у кнопок.
- Установщик перед обновлением просит академию закрыться (`CyberRangeCoach.exe stop`), старые сборки закрывает принудительно. В CI проверено обновление поверх запущенной академии.
</context>

<instructions>
1. Останови свой обход: процесс `wsl.exe -d Ubuntu -- sleep infinity`, который ты запускал вручную (найди его PID, затем `Stop-Process -Id <PID>`). Больше он не нужен.
2. Установщик (раздел ниже): скачай, сверь SHA-256 и поставь **поверх запущенной академии**, не закрывая её. Это проверка третьей ошибки: установка должна пройти без отката.
3. Запусти академию. Проверка первой ошибки: закрой все окна Ubuntu, подожди 2 минуты, потом «Система» → «Проверить SSH», затем «Проверить range-runner». Оба должны пройти с первого раза. Посмотри в Диспетчере задач, что `wsl.exe` висит один.
4. Вторая ошибка: сверь отпечаток из «Проверить SSH» командой, которую пишет сайт.
5. Закрой академию через Диспетчер задач (снять задачу) и проверь, что `wsl.exe` тоже исчез.
6. Ещё раз пройди урок «TCP-достижимость учебной цели» с Juice Shop.
7. Отчёт в `docs/noutbuk/otchety/<ГГГГММДД-ЧЧММ>.md`, коммит по имени, push. Потом `/zapomni`.
</instructions>

<constraints>
- Код академии на ноутбуке не править, ошибки описывать в отчёте.
- Docker, брандмауэр, `.wslconfig`, контролируемый доступ к папкам не менять без «да» владельца.
- Пароли вводит только владелец.
</constraints>

## Установщик

Сборка `9748358`, GitHub Actions run `36126697853`, артефакт `CyberRangeCoach-Windows-unsigned` (около 107 МБ).
SHA-256 `CyberRangeCoach-Setup.exe`: `7dcd0ba42248641db6233fed4b7bbc5b3d60b94a79f0bb2110d2ec98305fe501`.
Скачать: `gh run download 36126697853 -R Yerkebulan-Ardabayev/cyber-range-coach -n CyberRangeCoach-Windows-unsigned -D %USERPROFILE%\crc-installer-9748358`. Не совпала сумма → не ставить.
