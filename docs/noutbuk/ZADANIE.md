<context>
Ты на Windows-ноутбуке-полигоне Cyber Range Coach. Правила машины в `%USERPROFILE%\.claude\CLAUDE.md`, правила проекта в `CLAUDE.md` репозитория. Прошлый отчёт `docs/noutbuk/otchety/20260926-0810.md`: 422 исправлен, мягкая остановка получала запрос, но uvicorn бесконечно ждал открытые соединения страницы урока.

Исправлено в `1a20c31`: `timeout_graceful_shutdown=5` в `uvicorn.Config`, после 5 с сервер сам закрывает соединения. Тест на Mac держит незавершённый запрос во время `stop`; CI на Windows обновляет установщик при таком же открытом TLS-соединении и падает, если в журнале установщика есть «закрываем принудительно». Оба прошли.
</context>

<instructions>
1. Открой в браузере академию и лабу с терминалом (как в прошлый раз), чтобы были открытые соединения.
2. Установщик (раздел ниже): скачай, сверь SHA-256 и поставь поверх запущенной 1a2aeef с ключом `/LOG=<путь>`, не закрывая академию и страницу.
3. Проверь: в журнале установщика нет «закрываем принудительно»; в `academy.log` после «Получен запрос остановки» есть `Finished server process` (не позже примерно 10 с).
4. Запусти академию, убедись, что данные на месте (прогресс практики `pwd`, lab run 1 и 2).
5. Отчёт в `docs/noutbuk/otchety/<ГГГГММДД-ЧЧММ>.md` со строками обоих журналов, коммит по имени, push, потом `/zapomni`.
</instructions>

<constraints>
- Код академии на ноутбуке не править, ошибки описывать в отчёте.
- Docker, брандмауэр, `.wslconfig`, контролируемый доступ к папкам не менять без «да» владельца.
- Пароли вводит только владелец.
</constraints>

## Установщик

Сборка `1a20c31`, GitHub Actions run `36214460757`, артефакт `CyberRangeCoach-Windows-unsigned`.
SHA-256 `CyberRangeCoach-Setup.exe`: `d754c4ec08daded523039dab837076355100a050b1c6d50bd0814c74d2a68861`.
Скачать: `gh run download 36214460757 -R Yerkebulan-Ardabayev/cyber-range-coach -n CyberRangeCoach-Windows-unsigned -D %USERPROFILE%\crc-installer-1a20c31`. Не совпала сумма → не ставить.
