<context>
Ты работаешь на Windows-ноутбуке владельца (Windows 11 Pro).
На нём установлен учебный сайт Cyber Range Coach, сборка e1f189d. Это тренажёр по кибербезопасности для одного человека: уроки, в которых ученик сам вводит команды в Linux и проверяет учебные мишени.

Что уже работает (проверено 25.09.2026):
- Сайт открывается в браузере на этом ноутбуке (HTTPS, порт 8443). Если не открыт, запусти «Cyber Range Coach» из меню Пуск.
- Docker Desktop с двумя учебными мишенями: WebGoat (порт 8080) и Juice Shop (порт 3000). Оба отвечают.
- WSL с Ubuntu в режиме NAT, обычный пользователь Ubuntu `ye`. В Ubuntu уже стоят nmap, tcpdump, jq.
- В брандмауэре Windows уже есть правило для relay по диапазону адресов WSL 172.16.0.0/12. Relay это пересылка трафика с Windows к мишеням, через него урок из Ubuntu достаёт до мишени.

Что не сделано, и из-за этого стоит урок tcp-reachability: сайт пишет «Linux VM и SSH fingerprint ещё не подтверждены». Значит, мастер настройки Ubuntu ещё ни разу не пройден.
</context>

<instructions>
Задача из двух шагов. Делай сам всё, что можешь, владелец не хочет копировать команды.

Шаг 1. Пройти мастер WSL Ubuntu.
1. Открой сайт, слева раздел «Система», блок «Готовый профиль WSL Ubuntu», кнопка «Подготовить WSL Ubuntu».
2. Мастер создаёт в Ubuntu двух пользователей: `student` (от его имени ученик вводит команды) и `range-runner` (от его имени сайт проверяет ответы). Ещё он кладёт SSH-ключи и ставит недостающие учебные пакеты.
3. Если мастер ответит, что нужен sudo-пароль, он покажет готовую команду. Эту команду нужно выполнить внутри Ubuntu. Запусти её сам в терминале Ubuntu (`wsl`), а когда Ubuntu спросит пароль, остановись и попроси владельца ввести свой пароль Ubuntu прямо в этот терминал. Сам пароль не вводи, не спрашивай его в чате и никуда не записывай.
4. Вернись на сайт и пройди оставшиеся шаги мастера: проверка SSH, подтверждение отпечатка SSH-ключа, проверка проверяющего пользователя (runner-check). Готово, когда на сайте профиль Linux подтверждён без предупреждений.

Шаг 2. Проверить урок tcp-reachability.
1. Открой урок «TCP-достижимость учебной цели» (id tcp-reachability) с мишенью Juice Shop.
2. Пройди его как ученик: гипотеза, команда в терминале урока, ответ. Цель в том, чтобы урок запустился и засчитал правильный ответ, а трафик шёл через relay из Ubuntu.
3. Если урок не проходит, найди причину по сообщению сайта и журналу сайта (папка данных `%LOCALAPPDATA%\CyberRangeCoach\logs`).
</instructions>

<constraints>
- Контейнеры Docker, их образы и тома не запускать, не останавливать и не пересоздавать.
- Брандмауэр Windows, сетевые настройки и `.wslconfig` не менять без явного «да» владельца. Если без этого не работает, объясни одной фразой, что именно поменять и зачем, и жди ответа.
- Код сайта и установленные файлы программы не править. Найденную ошибку опиши в отчёте, исправлять её будут на Маке.
- Ничего не удалять.
</constraints>

<output>
Владельцу в чат пиши коротко и простыми словами, по-русски, без жаргона: что сделано и что нужно от него сейчас (одно действие за раз).

В конце сохрани отчёт в репозиторий, файл `docs/noutbuk/otchety/<ГГГГММДД-ЧЧММ>.md`, и отправь его (`git add` по имени файла, `git commit`, `git push`). Паролей и ключей в отчёт не писать, репозиторий публичный. В отчёте:
- прошёл ли мастер, и какой статус профиля Linux показывает сайт;
- прошёл ли урок tcp-reachability;
- каждая ошибка: точный текст с экрана, что делал перед этим, строки из журнала сайта;
- что поменял на ноутбуке (если менял) и с чьего разрешения.
</output>
