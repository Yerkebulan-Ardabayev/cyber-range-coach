<context>
Ты на Windows-ноутбуке-полигоне Cyber Range Coach. Правила машины в `%USERPROFILE%\.claude\CLAUDE.md`, правила проекта в `CLAUDE.md` репозитория (там новое правило: без строки `Co-Authored-By` в коммитах). Прошлый отчёт `docs/noutbuk/otchety/20260926-1148.md`, «Дополнение 11:55»: после перезапуска академии relay активной лабы не поднимался, `nc` висел.

Исправлено (`c1fa3eb`, `2d8c2c2`): при открытии терминала лабы и перед проверкой по probe академия поднимает relay активной лабы на том же порту. Если порт занят чужой программой, терминал пишет «Связь с учебной целью не восстановлена». Гонки со стопом лабы и запуском новой закрыты. critic-code PASS, CI PASS. На реальном WSL не проверено, это твоя задача.
</context>

<instructions>
1. Установщик (раздел ниже): скачай, сверь SHA-256, поставь поверх запущенной академии.
2. Начни лабу «TCP-достижимость учебной цели» с Juice Shop, в терминале `nc -vz 172.30.160.1 <порт из урока>` → succeeded.
3. Не завершая лабу, перезапусти академию (закрой через Диспетчер задач и запусти снова).
4. Открой ту же лабу. В терминале снова `nc -vz 172.30.160.1 <тот же порт>`: должно быть succeeded без «Начать чистую попытку». Проверь `Get-NetTCPConnection -LocalPort <порт>`.
5. Пройди проверку ответа до конца.
6. Отчёт в `docs/noutbuk/otchety/<ГГГГММДД-ЧЧММ>.md` с выводом команд и строками `academy.log`, коммит по имени без `Co-Authored-By`, push, потом `/zapomni`.
</instructions>

<constraints>
- Код академии на ноутбуке не править, ошибки описывать в отчёте.
- Docker, брандмауэр, `.wslconfig`, контролируемый доступ к папкам не менять без «да» владельца.
- Пароли вводит только владелец.
</constraints>

## Установщик

Сборка `2d8c2c2`, GitHub Actions run `36226716390`, артефакт `CyberRangeCoach-Windows-unsigned`.
SHA-256 `CyberRangeCoach-Setup.exe`: `419f28f635cb19399bd016232f84ef42c6109968baad0a20fb3ac09dfab30f61`.
Скачать: `gh run download 36226716390 -R Yerkebulan-Ardabayev/cyber-range-coach -n CyberRangeCoach-Windows-unsigned -D %USERPROFILE%\crc-installer-2d8c2c2`. Не совпала сумма → не ставить.
